 console.log(4>>1);
// 1 0 0 
// 0 1 0 => 2

// TERNARY OPERATIR

// SYNTAX: Condition ? what if true : if false 

// let userNumber=Number(prompt('enter a firstnumber'));
// let numberUser=Number(prompt('enter secondnumber'));
// console.log(userNumber>numberUser ? `${userNumber} is greater than ${numberUser}` : `${userNumber} is less than ${numberUser}`);
// alert(userNumber>numberUser ? `${userNumber} is greater than ${numberUser}` : `${userNumber} is less than ${numberUser}`);

// const USERNAME='bhawana';
// const PASSWORD= 'pandey';
// let username=(prompt('enter your USERNAME'));
// let password=Number(prompt('enter your PASSWORD'));
// console.log( USERNAME == username && PASSWORD == password ? " logged in " : " incorrect username OR password ");

// let greaterNumber=prompt('enter your age number');
// console.log(greaterNumber>=18 && greaterNumber<40 ? "you are valid user for voting" : "you are too early or too old for valid ");

// let firstNumber=Number(prompt('enter first num'));
// let secondNumber=Number(prompt('enter secondnum'));
// let result = firstNumber/secondNumber;
// console.log(result > 10 || result <= -1 ? 'result is greater than 10 OR result is negative'
//                                          : 'result is out of the scope');

// oddNumber = 19;
// console.log(oddNumber & 2 == 0 ? 'NUMBER IS EVEN' : 'NUMBER IS ODD');



//  ASSIGNMENT

//  *Basic Ternary Operator Assignments*
// 1. Check if a number is even or odd.
// 2. Determine the greater of two numbers.
// 3. Find the smallest of two numbers.
// 4. Check if a number is positive, negative, or zero.
// 5. Check if a number is divisible by 5.
// 6. Assign “Pass” or “Fail” based on marks.
// 7. Assign grade based on score (e.g., A if >90, else B).
// 8. Check if a person is eligible to vote (age ≥ 18).
// 9. Determine if a year is a leap year (basic check).
// 10. Check if a string is empty or not.
// 11. Assign “Eligible” or “Not Eligible” for scholarship (based on passing all 5 subjects).
// 12. Check if username and password are correct (simple string comparison).
// 13. **Assign “Day” or “Night” based on hour (24-hour format).
// 14. Set result to "Pass" or "Fail" based on average marks.
// 15. Check if a number is prime or not (using ternary inside a loop or function).

// Determine the greater of two numbers

// let userNumber=Number(prompt('enter a firstnumber'));
// let numberUser=Number(prompt('enter secondnumber'));
// console.log(userNumber>numberUser ? `${userNumber} is greater than ${numberUser}` : `${userNumber} is less than ${numberUser}`);
// alert(userNumber>numberUser ? `${userNumber} is greater than ${numberUser}` : `${userNumber} is less than ${numberUser}`);

// Find the smallest of two numbers.
